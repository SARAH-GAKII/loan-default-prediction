# loan-default-prediction
Loan default prediction for home equity loans using the HMEQ dataset, leveraging a structured ML pipeline with EDA, missing data handling, and classification models (logistic regression, decision tree, random forest). Emphasizes model comparison under class imbalance using precision, recall, F1-score, and ROC-AUC.
